# Session 9: screenshots still to capture

S01 to S12 and S14 exist (see CAPTURED.md). S13 renders as a grey
placeholders with the S-number printed on them until the file is
replaced. Capture at ~1600 px wide, light theme, cropped tight,
circles in `#b31b1b`, landscape (h/w <= 0.7) for the two-column slides.

| # | Page / state | What is circled | Slide |
|:--|:--|:--|:--|
| S13 | RStudio's Terminal pane with the terminal dropdown open: *Terminal 1* (Claude Code running) and *Terminal 2* (shell prompt) | the dropdown; *Terminal 2* | Keys, commands, and a second Terminal tab (page only) |
| S15 | *(optional; removed from the page 2026-09-15 so Before class carries no grey box)* the New Project dialog: *New Directory → New Project*, name `gapminder-practice`, subdirectory `~/github`, **Create a git repository** ticked | the name box; the checkbox; **Create Project** | Before class, step 4 (page only), if the instructor wants it back |
| S17 | *(optional; removed from the page 2026-09-15)* RStudio's Terminal tab with `claude --version` and its output | the output line | Before class, step 3 (page only), if wanted |
| S18 | *(not on the page; would settle Before class step 6)* one fresh first run of `claude` on a Pro account: the set-up questions, the login-method menu, the browser sign-in, the trust question, in the order they appear | nothing; the order of the screens is the point | Before class, step 6 |

File name: `S13-terminal-dropdown.png`.
S15 and S17 (grey placeholders) were taken off
the page on 2026-09-15; the files stay in the folder. S16 (the Files pane
after setup) and S18 (the sign-in sequence) were in the outline and are
not on the page.
