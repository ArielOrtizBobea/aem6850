The attached file is gapminder.csv: one row per country and year; columns country, continent, year, lifeExp, pop, gdpPercap; 1704 rows; 142 countries; years 1952 to 2007 every five years. In my RStudio project it sits at data/gapminder.csv.

Goal: the population-weighted mean of lifeExp for each continent in 2007.

Write an R script I will save as code/2_lifeexp_weighted_2007.R. Base R only, no packages. Read the file with read.csv("data/gapminder.csv"). Keep the rows for 2007 first; the file has twelve years per country. For each continent compute sum(lifeExp * pop) / sum(pop); do not take the simple mean across countries. Write output/lifeexp_weighted_2007.csv with the columns continent and weighted_lifeExp, and end with cat() lines that print the five values rounded to two decimals.

Do not compute the numbers here and do not paste a table; I will run the script myself. Do not invent columns. If the file does not match what I described, stop and say so.
