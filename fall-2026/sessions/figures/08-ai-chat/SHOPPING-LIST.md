# Session 8: screenshots and transcripts still to capture

Captured 2026-09-14/15 and removed from this list: S01, S02, S03, S04, S05,
S10, S14 and the transcripts T01, T04, T05 (see CAPTURED.md); the page
quotes them. Every file below is still a grey placeholder (the S-number
printed on it) so the page renders. Capture at ~1600 px wide, light theme,
cropped tight, circles and numbers in `#b31b1b`; landscape (height / width
<= 0.7) for every shot that sits in a two-column slide. Replace the
placeholder at the same path and delete the row. The Slide column names
the `##` section of `08-ai-chat.qmd` the shot belongs to. The rows T06a-c,
T07 and T08 are optional: the revised page has no slot for them.

## Screenshots

| # | Page / state | What is circled | Slide | Exact state or prompt to capture |
|:--|:--|:--|:--|:--|
| S06 | the Claude desktop app on a Pro account, signed in, Cowork visible in the header or composer | **Cowork** (label copied from the screen) | Cowork: an assistant that reaches your files | Fresh app window, nothing typed. Copy the exact label into the slide's first item. |
| S07 | Cowork with `github/gapminder-practice` connected, before any prompt | the folder's name where the window shows it, the task box, the stop control, the approval-mode setting | Connecting the folder | Folder connected, approval mode at the setting that asks before acting. Copy the four labels into the slide. |
| S11 | *(page only)* claude.ai Settings > Capabilities, the code-execution switch on | the switch | Attaching the CSV (page) | Settings > Capabilities. Record the exact menu labels; the page states the Help Center's names. |
| S12 | *(page only)* the desktop app signed in on a **free** account: Chat only, no Cowork | the header or composer where Cowork would be | Cowork: an assistant that reaches your files (page) | Needs a free account (a TA's). |
| S13 | *(page only)* claude.ai, a new chat, the `gapminder.csv` chip visible, P7 (the chat version of the chore) in the message box with `notes.md` pasted under it, before sending | the chip and the message box | The same chore in chat, without Cowork (page) | Do not send. P7 is on the page under that heading. |

## Transcripts

| # | Where it goes | Exact prompt (send in a new chat, no attachment unless stated) | What to record |
|:--|:--|:--|:--|
| T06a, T06b, T06c | The three prompts in full (page) | The three prompts under that heading; T06b with the real `str(d)` and `head(d, 3)` output pasted | Each reply trimmed to eight lines. |
| T07 | Explaining an error message (page) | The prompt on that slide, with the `lifeexp` line and the two error lines included | The sentence that names the case of `lifeExp` and the fixed line. |
| T08 | Practice 1 solution (optional) | The error prompt with `read.csv("data/gapmnder.csv")` and R's error and warning pasted under it | Four lines. |

## Not needed any more

S08 (Cowork after the chore), S09 (the trend plot in the Files pane),
T02a/T02b (the answer and the mini script), T03 (the Cowork chore run),
T09 (`git status` after it): replaced by the live demo (decision 10,
2026-09-14). The demo's own fallback is captured and tested:
`demo/wiki_lifeexp_map.R` and `demo/lifeexp_map.png`.
